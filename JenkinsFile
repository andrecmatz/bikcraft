pipeline {
    agent none
    stages {
        stage('Build') {
            agent {
                docker {image 'maven' }
            }
            steps {
                sh ''mvh package -Dmaven.test.skip=true''
            }
        }
    }
}